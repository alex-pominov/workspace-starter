{
  "name": "@workspace-starter/two",
  "private": true,
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "webpack serve --config config/webpack.config.js --mode development --port 5001",
    "lint": "yarn workspace @workspace-starter/common lint-exec $INIT_CWD/src",
    "test": "yarn workspace @workspace-starter/common jest-config && jest --rootDir=."
  },
  "dependencies": {
    "@workspace-starter/common": "1.0.0"
  },
  "peerDependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
